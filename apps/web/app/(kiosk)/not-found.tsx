/** Unknown city slug on the projector — keep the board blank of directory chrome. */
export default function KioskNotFound() {
  return (
    <div className="flex min-h-dvh items-center justify-center bg-paper px-8">
      <p className="font-listing text-5xl uppercase tracking-tight">Live feed</p>
    </div>
  )
}
