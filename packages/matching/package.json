{
  "name": "@rmm/matching",
  "version": "1.0.0",
  "description": "Pure, deterministic, explainable match scoring for the Recoding Medicine Matchmaker. Zero framework dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rmm/schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.7.3",
    "vitest": "^2.1.8"
  }
}
